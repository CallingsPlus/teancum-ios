import Foundation

public enum PrayerStatistic: String {
    case contact = "contacts"
    case accept = "accepts"
    case cancel = "cancels"
    case decline = "declines"
    case miss = "misses"
}
